import { createTheme } from "@mui/material";

export const customeTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#004e98",
    },
    secondary: {
      // main: "#a1a09b",
      main: "#fe012b",
    },
  },
});
